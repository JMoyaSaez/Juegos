# Tools

No build step is required.

Open `index.html` directly in Chrome/Edge.

For quick syntax validation from this folder:

```bash
node --check src/core/config.js
node --check src/scenes/game.js
```
